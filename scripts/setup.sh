#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

printf "Arbiter setup check\n"
printf "repo: %s\n\n" "${ROOT_DIR}"

printf "TBD: setup steps will be added after the compiler/runtime skeleton is introduced.\n"
